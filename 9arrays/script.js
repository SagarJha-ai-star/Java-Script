

// array = a variable like structure that can hold
//         more than 1 value

let fruits = ["apple", "orange", "banana", "coconut"];

//fruits .push("coconut"); push element to end
//fruits.pop(); pop element from end
//fruits.unshift("mango"); push element in front
//fruits.shift(); remove element from starting
//nums.sort();
//nums.reverse();

let numOfFruits = fruits. length; 
let index = fruits.indexOf("apple"); //finds index number of a element and returns  -1 if element not found

console.log(index);
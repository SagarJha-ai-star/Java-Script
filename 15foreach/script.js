// foreach = method used to iterate over the elements of Array
//           and apply A specified function(callback) to each element

let nums = [1, 2, 3, 4, 5];
nums.forEach(display);

function display(element){
    console.log(element);
}